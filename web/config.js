// 部署前填好這兩個值（兩者都不是機密，可以公開放在 GitHub Pages 上）
window.ACD_CONFIG = {
  // Apps Script「部署為網頁應用程式」後拿到的網址，結尾是 /exec
  API_URL: '',
  // Google Cloud Console → API 和服務 → 憑證 → OAuth 用戶端 ID（網頁應用程式）
  GOOGLE_CLIENT_ID: ''
};
